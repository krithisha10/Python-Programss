d = int(input("Days late: "))

if d <= 5:
    fine = d * 0.5
elif d <= 10:
    fine = (5*0.5) + (d-5)*1
elif d <= 30:
    fine = (5*0.5) + (5*1) + (d-10)*5
else:
    print("Membership cancelled")
    fine = (5*0.5) + (5*1) + (d-10)*5

print("Fine =", fine)

Output:
Days late: 4
Fine = 2.0
